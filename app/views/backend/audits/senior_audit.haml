= render :partial => "info"
.well
  请确认所有信息属实，符合发标条件
  = link_to "通过终审，预备发标", senior_audit_pass_backend_audit_path(@loan), method: :post, :class=>"btn btn-primary", :confirm => '确认符合发标条件？'
  &nbsp;&nbsp;
  = link_to "编辑", edit_backend_loan_path(@loan), method: :get, :class=>"btn btn-primary"